[package]
name = "mafold-transcript"
version = "0.1.0"
edition = "2021"
description = "The agent→chat transcript layer: one event vocabulary, one card renderer, one grouping state machine — shared by the self-hosted daemon and the server-side brains."
license = "MIT"

[dependencies]
serde_json = "1"
